# 会话交接 · 2026-08-30

> 给接手智能体：读完本节 + 知识库/索引.md 即可完整接手本项目。本文件是 2026-08-30 会话的浓缩交接。

## 一、一句话状态

- 小红书生图方案**彻底改版**：放弃 Canva AI 直出（8卡效果差）+ 长图视觉分割（内容被切断），改为 **HTML 卡片渲染**（`小红书卡片渲染.py`，封面A+5正文卡，1242×1660）。
- automation 已重建并收窄：**只跑步骤1-3.5 停在选文审核门**，步骤4-9 对话内续跑。
- 新增 `HTML转内容文本.py`：生成完整版 + `--brief` 简化版（小红书 1000 字内）。
- 多平台发布**仅完成调研**，未落地（候选 MultiPost，需 multipost.app 账号+apiKey）。

## 二、今日产出/变更（新文件）

| 文件 | 说明 |
|------|------|
| 工作流/小红书卡片渲染.py | **核心**：公众号 HTML → 1242×1660 小红书卡片（封面A大标题品牌色 + 5张正文卡，路径B重排） |
| 工作流/HTML转内容文本.py | HTML → 纯文本结构（完整版全部章节 + `--brief` 简化版，小红书1000字内） |
| 工作流/小红书长图分割.py | 长图视觉分割（内容块被切断，**已否决/废弃**） |
| 工作流/小红书HTML分卡渲染.py | HTML 切块渲染中间方案（被卡片渲染替代，**废弃**） |
| 知识库/自动化任务.md | 更新：v2.15 生图方案、版本历史 |
| 知识库/脚本清单.md | 更新：8 核心脚本 + 废弃清单 |
| 总纲 7步完整流程.md | v2.14 → **v2.15**（步骤6 改 HTML 渲染、步骤6.5 去水印退役） |

## 三、关键决策（今日新增）

1. **小红书生图 = HTML 卡片渲染**（`小红书卡片渲染.py`）：按公众号 HTML 的 h2 章节重排成卡片，文字 100% 准、版式可控。停用 Canva 直出 + 长图分割。
2. **封面 = A 版（大标题+品牌色 #D0021B）**，B 版（沿用公众号头部）备选。
3. **正文 = 5 张卡**：骨架/规范词/论证骨架(3分论点合一)/万能句式(3句式合一)/速记+行动(合一)。用户明确"暂不细分"（分论点/句式独立成卡已设计但暂缓）。
4. **去水印退役**：HTML 渲染无水印，LaMa 仅公众号封面备用。
5. **automation 范围收窄**（v2.14）：06:30 只跑步骤1-3.5，停在选文审核门；步骤4-9 对话续跑。
6. **选文审核通道**：企微只发通知（wecom-cli 不能收），审核在 WorkBuddy 对话回复"通过/换文"；换文给版次+标题由自动化抓取。
7. **小红书纯文本正文**：`HTML转内容文本.py --brief`，限制 1000 字（小红书按**含空格**统计）；精简规则见脚本内注释 + 决策与约定.md。
8. **多平台发布选型**：MultiPost（RESTful API，但实为云端 multipost.app 中转需 apiKey）为候选；公众号/视频号无法自动发布，小红书有封号风险。

## 四、卡点 / 风险

1. **小红书字数统计口径不明**：本地"含空白 651 字"对应小红书显示约 881 字（待用户实测确认）。差异来自小红书对空格/换行/标点的统计方式。
2. **MultiPost RESTful API 未落地**：确认是云端 multipost.app（需 apiKey + 注册账号 + 链接扩展），非本地 localhost 服务；网上 `localhost:3000/api/publish` 示例是博主推断，源码只有 `/api/extension/ping`。
3. automation 依赖状态.json 断点续跑，步骤4-9 需用户在对话主动触发续跑，不会自动。

## 五、待办（优先级）

- **P0** 用户实测小红书简化版字数，若仍超 1000 需继续精简（可砍：骨架速览/原文摘录/行动清单/规范词减到1个）
- **P1** 多平台发布落地：确认发布方式（半自动弹窗/全自动）+ 内容差异化（三平台统一 vs 定制），再写 `多平台发布.py`
- **P2** 小红书卡片细分（分论点/句式独立成卡）——已设计，用户暂缓

## 六、接手第一步（按顺序）

1. 读 `知识库/索引.md`（9 步接手流程）
2. 读 `工作流/人民日报内容运营_7步完整流程.md`（v2.15 总纲）
3. 读 `工作流/小红书卡片渲染.py` + `工作流/HTML转内容文本.py`（两个新核心脚本）
4. 读 `知识库/自动化任务.md`（automation 状态 + 生图方案）
5. 确认 automation `automation-1788016644530` 状态（周一~五 06:30）
6. 处理用户新需求（多平台发布 / 简化版字数）

## 七、相关文件索引

| 文件 | 用途 |
|------|------|
| 工作流/人民日报内容运营_7步完整流程.md | 总纲 SOP v2.15 |
| 工作流/小红书卡片渲染.py | 小红书卡片渲染（生产） |
| 工作流/HTML转内容文本.py | 纯文本完整版/简化版 |
| 知识库/索引.md | 自足入口（九步接手） |
| 知识库/自动化任务.md | automation 配置/生图方案 |
| 知识库/决策与约定.md | 用户拍板 21 条 |
